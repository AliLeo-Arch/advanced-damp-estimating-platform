# Frontend env for Vite (copy to `.env.local` for local override)

# Leave empty for local Vite (uses proxy to http://127.0.0.1:8000).
# On Vercel, set this to your backend deployment URL (no trailing slash):
# VITE_API_URL=https://your-backend.vercel.app
VITE_API_URL=
